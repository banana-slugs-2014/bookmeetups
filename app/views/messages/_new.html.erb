<%= form_for [@meetup, @message], remote: true do |f| %>
  <%= hidden_field_tag(:user_id, current_user.id)  %>
  <%= f.text_area :text, :required => true %>
  <%= submit_tag "New Message" %>
<% end %>
<%= link_to "Cancel", root_path, :class=> "cancel" %>

