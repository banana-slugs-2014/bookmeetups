<h1> Results </h1>
<ul>
  <% @books.each do |book| %>
    <li>
      <div class="book">
        <ul>
          <li><%= book.title  %></li>
          <li><%= book.author %></li>
          <li><img src="<%= book.photo_link(1) %>" ></li>
        <ul>
      </div>
    </li>
  <% end %>
</ul>

