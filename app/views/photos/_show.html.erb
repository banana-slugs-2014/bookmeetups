<% if user.photo.nil? %>
  <%= image_tag src="/assets/defaults/default_#{user.id % 129}.jpg", :class => "default_profile" %>
<% else %>
  <%= image_tag serve_user_photo_path(user.id, user.photo.id) %>
<% end %>