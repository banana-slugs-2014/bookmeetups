<p>Book collection: </p>

<div class="book_list">
  <% @books.each do |book| %>
    <div class="book-display">
      <h4><%= book.title %></h4>
      <%= link_to image_tag(book.photo_link(2)), book_path(book) %>
    </div>
  <% end %>
</div>